#ifndef ENVIRONMENT_H
#define ENVIRONMENT_H

/**
 * @brief A class to store environment information for the type checkers.
 * Includes a registry of all structs and functions.
 * Also tracks namespaces in a tree.
 *
 */
class Environment {
};

#endif // ENVIRONMENT_H
